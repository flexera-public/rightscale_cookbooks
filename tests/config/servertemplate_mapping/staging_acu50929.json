/*
 * These are the STAGING ServerTemplates which are used for development.
 *
 * This uses the HEAD revisions of the ServerTemplates.
 */
{
  "base_chef": {
    "nickname": "Base ServerTemplate for Linux STAGING acu50929",
    "rev": "head"
  }
}
