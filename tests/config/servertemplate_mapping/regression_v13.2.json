/*
 * These are the v13.2 regression ServerTemplates which was done 2012-11-29 to 2012-12-14.
 *
 * This uses the latest versions of the ServerTemplates with v13.2 in their name.
 */
{
  "base_chef": {
    "nickname": "Base ServerTemplate for Linux (v13.2)",
    "rev": "latest"
  },
  "base_rsb": {
    // The nickname contained two spaces between "(RSB)" and "(v13.2)"
    "nickname": "Base ServerTemplate for Linux (RSB)  (v13.2)",
    "rev": "latest"
  }
}
